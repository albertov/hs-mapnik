language: nix
sudo: false

env:
  - RECIPE=with_710
  - RECIPE=with_802
  - RECIPE=with_821

os:
  - linux
  - osx


script:
  - git clone --depth=1 https://github.com/NixOS/nixpkgs
  - nix-build -A ${RECIPE} travis.nix
