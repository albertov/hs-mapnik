-- | This module is so ghci can find it. Installed version of
-- the library uses the module auto-generarated by Cabal
module Paths_hs_mapnik where

getDataFileName :: FilePath -> IO FilePath
getDataFileName = return
